export const SET_COUNTRIES = (state, countries) => {
  state.countries = countries;
};

export const SET_STATE = (state, states) => {
  state.states = states;
};

export const SET_CITY = (state, cities) => {
  state.cities = cities;
};

export const SET_DEST_STATE = (state, states) => {
  state.destStates = states;
};

export const SET_DEST_CITY = (state, cities) => {
  state.destCities = cities;
};

export const SET_SHIPMENT = (state, shipment) => {
  state.shipment = shipment;
};
