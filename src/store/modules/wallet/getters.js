export const wallets = (state) => {
  return state.wallets;
};

export const wallet = (state) => {
  return state.wallet;
};

export const get_paystack_details = (state) => {
  return state.paystackDetail;
};
