// export const SET_WALLETS = (state, wallets) => {
//   state.wallets = wallets;
// };

export const SET_WALLET = (state, wallet) => {
  state.wallet = wallet;
};

// export const UPDATE_PAYSTACK_DETAIL = (state, payment) => {
//   state.wallet = payment;
// };
